# Solo Mode

The human is architecture, integration, and release authority. Agents may act as bounded Builder, Reviewer, QA, or explainer roles.

Parallelize execution only when separate worktrees protect concurrent mutable tasks. Keep one integration owner: the human. Use short rendezvous checkpoints after contract or dependency merges.
